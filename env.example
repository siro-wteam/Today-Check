# Supabase Configuration
# Copy this file to .env.local and fill in your actual values

EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Google Places API key for task location autocomplete.
# - Native app: set EXPO_PUBLIC_GOOGLE_PLACES_API_KEY (client calls Google directly).
# - Web: use Supabase Edge Function (avoids CORS). Set the key as a Supabase secret and deploy:
#     supabase secrets set GOOGLE_PLACES_API_KEY=your-google-places-api-key
#     supabase functions deploy places-autocomplete
# EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=your-google-places-api-key

# Optional: Privacy Policy URL for native apps and App Store Connect.
# Set to your deployed web app URL + '/privacy' (e.g. https://todaycheck.app/privacy).
# If not set: on web the in-app /privacy page is used; on native the policy is shown in-app.
# EXPO_PUBLIC_PRIVACY_POLICY_URL=https://your-domain.com/privacy
