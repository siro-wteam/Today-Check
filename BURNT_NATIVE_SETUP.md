# Burnt 토스트 — iOS/Android 네이티브 설정

Burnt는 **네이티브 모듈**이라 **Expo Go에서는 동작하지 않습니다**.  
iOS/Android에서 토스트 + 햅틱을 쓰려면 **Development Build**를 만들어서 실행해야 합니다.

---

## 이미 해둔 것 ✅

- `expo-build-properties` 설치
- `app.json`에 플러그인 추가 및 iOS `deploymentTarget: "13.0"` 설정

---

## 당신이 할 일 (순서대로)

### 1. 네이티브 프로젝트 생성 (prebuild)

```bash
npx expo prebuild --clean
```

- `ios/`, `android/` 폴더가 생깁니다.
- `--clean`: 기존 `ios`/`android`가 있으면 지우고 다시 만듭니다.

### 2. 시뮬레이터/에뮬레이터에서 실행

**iOS (Mac만 해당):**

```bash
npx expo run:ios
```

- Xcode + iOS 시뮬레이터가 필요합니다.
- 시뮬레이터가 없으면 Xcode → Window → Devices and Simulators 에서 추가.

**Android:**

```bash
npx expo run:android
```

- Android Studio + Android 에뮬레이터(또는 실제 기기)가 필요합니다.

---

### 3. 실제 핸드폰에서 실행하기

#### iOS (iPhone)

**준비사항:**
1. **macOS Developer Mode 활성화** (필수!)
   - 시스템 설정 (System Settings) 열기
   - **개인정보 보호 및 보안** (Privacy & Security) 클릭
   - **Developer Mode** 찾기
   - 토글을 **켜기** (ON)
   - Mac 재시작 요청이 나오면 재시작
   - 재시작 후 확인 팝업에서 **"켜기"** 선택
2. iPhone을 Mac에 USB로 연결
3. iPhone에서 "이 컴퓨터를 신뢰" 선택
4. Xcode가 설치되어 있어야 함

**실행:**

```bash
npx expo run:ios --device
```

- 연결된 기기 목록이 나오면 기기를 선택
- **처음 실행 시:** Xcode에서 개발자 계정으로 서명 필요
  - Xcode → Preferences → Accounts → Apple ID 추가
  - 프로젝트 열기: `open ios/TodayCheck.xcworkspace`
  - Signing & Capabilities → Team 선택

**기기에서 Developer Mode 활성화 (iOS 16 이상 필수!):**

⚠️ **중요:** iOS 16 이상에서는 기기 자체의 Developer Mode를 활성화해야 합니다!

1. **iPhone에서 설정 앱 열기**

2. **Developer Mode 찾기:**
   - **개인정보 보호 및 보안** (Privacy & Security) 클릭
   - 아래로 스크롤하여 **Developer Mode** 찾기
   - 없으면 검색창에 **"Developer"** 또는 **"개발자"** 입력

3. **Developer Mode 활성화:**
   - **Developer Mode** 토글을 **켜기** (ON)로 변경
   - iPhone 재시작 요청이 나타나면 **재시작** 선택

4. **재시작 후 확인:**
   - 재시작 후 확인 팝업이 나타나면 **"켜기"** 또는 **"Enable"** 선택
   - 비밀번호 입력 요청이 나오면 iPhone 잠금 비밀번호 입력

**기기에서 앱 신뢰하기:**

1. **앱을 처음 실행할 때:**
   - iPhone에서 앱 아이콘을 탭하면 "신뢰할 수 없는 개발자" 경고가 나타남
   - 경고에서 **"설정"** 버튼을 탭

2. **또는 수동으로:**
   - 설정 앱 열기
   - **일반** → **VPN 및 기기 관리** (또는 **기기 관리**)
   - **개발자 앱** 섹션에서 개발자 계정 선택
   - **"[Your Name] 신뢰"** 버튼 탭
   - 확인 팝업에서 **"신뢰"** 선택

3. **앱 다시 실행:**
   - 홈 화면에서 앱 아이콘을 다시 탭하면 정상 실행됨

**또는 Xcode에서 직접:**
```bash
open ios/TodayCheck.xcworkspace
```
- 상단에서 기기 선택 → ▶️ 버튼 클릭

---

#### Android

**준비사항:**
1. Android 기기를 USB로 연결
2. 기기에서 **개발자 옵션** 활성화:
   - 설정 → 휴대전화 정보 → 빌드 번호를 7번 탭
3. **USB 디버깅** 활성화:
   - 설정 → 개발자 옵션 → USB 디버깅 ON

**실행:**

```bash
npx expo run:android --device
```

- 연결된 기기 목록이 나오면 기기 선택
- 기기에서 "USB 디버깅 허용" 팝업 → 확인

---

### 4. 빌드 후에도 개발 서버 연결하기 ⚠️ 중요!

**Xcode에서 빌드만 하면 앱이 설치되지만, JavaScript 번들을 로드하려면 Metro bundler가 필요합니다.**

기기에 앱이 설치된 후, **반드시** 개발 서버를 실행해야 합니다:

```bash
npx expo start --dev-client
```

또는:

```bash
npm start
```

**확인 사항:**
1. ✅ 앱이 아이폰에 설치되었는지 확인 (홈 화면에 아이콘 확인)
2. ✅ Metro bundler가 실행 중인지 확인 (터미널에 "Metro waiting on..." 메시지)
3. ✅ 아이폰과 Mac이 같은 Wi-Fi 네트워크에 연결되어 있는지 확인
4. ✅ 앱을 실행했을 때 Metro bundler에 연결 요청이 오는지 확인

**앱이 여전히 동작하지 않으면:**

**1. Metro bundler 상태 확인:**
   - 터미널에서 "Metro waiting on..." 메시지가 보이는지 확인
   - 보이지 않으면 Metro bundler가 실행되지 않은 것입니다

**2. Metro bundler 재시작:**
   ```bash
   # 기존 프로세스 종료 (터미널에서 Ctrl+C)
   # 그 다음 다시 실행:
   npx expo start --dev-client
   ```
   - 또는 `npm start` 실행

**3. 네트워크 연결 확인:**
   - ✅ 아이폰과 Mac이 **같은 Wi-Fi 네트워크**에 연결되어 있는지 확인
   - ✅ 방화벽이 Metro bundler를 차단하지 않는지 확인
   - ✅ VPN이 켜져 있으면 끄고 시도

**4. USB를 통한 연결 시도:**
   - Wi-Fi 연결이 안 되면 USB로 연결:
   ```bash
   npx expo start --dev-client --tunnel
   ```
   - 또는 Xcode에서 직접 실행 (Xcode가 자동으로 연결)

**5. 앱 완전 종료 후 재실행:**
   - 아이폰에서 앱 완전 종료 (앱 스위처에서 위로 스와이프)
   - 다시 실행

**6. Xcode 콘솔 확인:**
   - Xcode 하단 콘솔에서 에러 메시지 확인
   - Metro bundler 연결 관련 로그 확인

---

## 개발 워크플로우 (효율적인 개발 방법)

### 첫 빌드는 오래 걸립니다 ⏱️

**정상입니다!** 첫 빌드는:
- 네이티브 코드 컴파일 (Swift/Objective-C)
- 모든 의존성 라이브러리 빌드
- 앱 번들 생성 및 서명
- **보통 5-15분 정도 걸립니다**

**이후 빌드는 훨씬 빠릅니다:**
- 변경된 부분만 다시 빌드 (증분 빌드)
- 보통 1-3분 정도

### 효율적인 개발 방법 🚀

**1. 첫 빌드 후 Metro bundler 실행:**

```bash
npx expo start --dev-client
```

**2. JavaScript 코드 변경 시:**
- ✅ **네이티브 빌드 불필요!**
- ✅ **Fast Refresh 자동 적용** (저장하면 즉시 반영)
- ✅ **1-2초 내 변경사항 확인 가능**

**3. 네이티브 코드 변경 시에만:**
- 네이티브 모듈 추가/수정
- `app.json` 설정 변경
- Podfile 변경
- → 이 경우에만 Xcode에서 다시 빌드 필요

### 개발 시나리오별 가이드

**시나리오 1: JavaScript/TypeScript 코드만 수정**
```
1. Metro bundler 실행 중 유지
2. 코드 수정 → 저장
3. 앱에서 자동으로 새로고침 (Fast Refresh)
→ 네이티브 빌드 불필요! ⚡
```

**시나리오 2: 네이티브 모듈 추가 (예: 새 라이브러리)**
```
1. npm install [라이브러리]
2. pod install (필요시)
3. Xcode에서 다시 빌드
→ 이 경우에만 오래 걸림 ⏱️
```

### IP 주소 자동 감지 스크립트 사용 🎯

매번 IP 주소를 수동으로 입력하지 않고 자동으로 감지하여 실행할 수 있습니다:

**방법 1: npm 스크립트 사용 (권장)**
```bash
npm run ios:device
```

**방법 2: 스크립트 직접 실행**
```bash
./scripts/run-ios-device.sh
```

**스크립트 동작:**
- 자동으로 Mac의 IP 주소를 감지 (en0, en1, en2 순서로 시도)
- 감지된 IP를 `REACT_NATIVE_PACKAGER_HOSTNAME` 환경 변수로 설정
- `npx expo run:ios --device` 실행

**IP를 찾을 수 없는 경우:**
- Wi-Fi 또는 이더넷이 연결되어 있는지 확인
- 네트워크 설정에서 IP 주소 확인

**시나리오 3: 앱이 크래시하거나 이상하게 동작**
```
1. Metro bundler 재시작: Ctrl+C → npx expo start --dev-client
2. 앱 완전 종료 후 다시 실행
3. 그래도 안 되면 Xcode에서 Clean Build (⇧⌘K) 후 다시 빌드
```

### 개발 팁 💡

1. **Metro bundler는 항상 실행 상태로 유지**
   - 터미널을 별도로 열어서 계속 실행
   - 코드 변경 시 자동으로 반영됨

2. **Xcode는 필요할 때만 사용**
   - 첫 빌드, 네이티브 변경, 디버깅 시에만

3. **빌드 시간 단축:**
   - 시뮬레이터 사용 (실제 기기보다 빠름)
   - Release 빌드 대신 Debug 빌드 사용
   - 불필요한 의존성 제거

**요약:**
- 첫 빌드: 5-15분 (정상)
- 이후 JavaScript 변경: 즉시 반영 (Fast Refresh)
- 네이티브 변경: 다시 빌드 필요 (1-3분)

---

## 트러블슈팅

### macOS: "Developer Mode disabled" 오류

**증상:**
```
Developer Mode disabled
To use Siro for development, enable Developer Mode in Settings → Privacy & Security.
```

**해결 방법:**

**방법 1: 시스템 설정에서 찾기**

1. **시스템 설정 열기**
   - Apple 메뉴 (🍎) → **시스템 설정** (또는 **System Settings**)
   - 또는 Spotlight 검색 (⌘Space) → "시스템 설정" 입력

2. **Developer Mode 찾기 (여러 방법 시도)**
   
   **방법 A: 검색 사용**
   - 시스템 설정 상단 검색창에 **"Developer"** 또는 **"개발자"** 입력
   - "Developer Mode" 또는 "개발자 모드" 선택
   
   **방법 B: 직접 찾기**
   - 왼쪽 사이드바에서 **개인정보 보호 및 보안** (Privacy & Security) 클릭
   - 아래로 스크롤하여 **Developer Mode** 찾기
   - 없으면 **보안** (Security) 섹션 확인

**방법 2: Xcode를 먼저 실행하기**

Developer Mode 옵션이 보이지 않으면, Xcode를 한 번 실행해보세요:

```bash
open -a Xcode
```

- Xcode가 처음 실행되면 라이선스 동의 및 추가 설정이 필요할 수 있습니다
- Xcode 실행 후 시스템 설정을 다시 확인하면 Developer Mode가 나타날 수 있습니다

**방법 3: 터미널에서 직접 활성화 (시스템 설정에 없을 때)**

시스템 설정에서 Developer Mode를 찾을 수 없고 `DevToolsSecurity -status`가 "disabled"를 보여주면:

1. **터미널에서 직접 활성화 시도:**
   ```bash
   sudo DevToolsSecurity -enable
   ```
   - Mac 로그인 비밀번호 입력 필요
   - 성공하면 "Developer mode is currently enabled" 메시지가 나옵니다

2. **또는 Xcode에서 실제 기기 빌드 시도:**
   - Xcode에서 실제 기기로 빌드/실행을 시도하면
   - 자동으로 Developer Mode 활성화 프롬프트가 나타날 수 있습니다
   - 프롬프트가 나타나면 "켜기" 선택

**방법 4: 이미 활성화되어 있을 수 있음**

Developer Mode가 이미 활성화되어 있으면 시스템 설정에 나타나지 않을 수 있습니다. 이 경우:
- Xcode에서 다시 빌드 시도
- 또는 터미널에서 확인:
  ```bash
  DevToolsSecurity -status
  ```
  - "Developer mode is currently enabled"가 나오면 이미 활성화된 것입니다

**활성화 후:**

3. **Mac 재시작**
   - 재시작 요청 팝업이 나타나면 **재시작** 선택
   - 또는 수동으로 재시작: Apple 메뉴 → 재시작

4. **재시작 후 확인**
   - 재시작 후 확인 팝업이 나타나면 **"켜기"** 또는 **"Enable"** 선택
   - 비밀번호 입력 요청이 나오면 Mac 로그인 비밀번호 입력

**참고:**
- macOS Ventura (13.0) 이상에서만 이 기능이 있습니다
- 이 설정은 실제 기기에 앱을 설치할 때 필요합니다
- 시뮬레이터에서는 필요 없습니다
- Xcode를 처음 설치한 경우, 한 번 실행해야 Developer Mode 옵션이 나타날 수 있습니다

### iOS: "Developer Mode disabled" 오류 (기기에서)

**증상:**
```
Developer Mode disabled
To use Siro for development, enable Developer Mode in Settings → Privacy & Security.
```

**중요:** 이것은 **Mac의 Developer Mode가 아니라 iPhone의 Developer Mode**입니다!

**해결 방법:**

1. **iPhone에서 설정 앱 열기**

2. **Developer Mode 찾기:**
   - **개인정보 보호 및 보안** (Privacy & Security) 클릭
   - 아래로 스크롤하여 **Developer Mode** 찾기
   - 없으면 검색창에 **"Developer"** 또는 **"개발자"** 입력

3. **Developer Mode 활성화:**
   - **Developer Mode** 토글을 **켜기** (ON)로 변경
   - iPhone 재시작 요청이 나타나면 **재시작** 선택

4. **재시작 후 확인:**
   - 재시작 후 확인 팝업이 나타나면 **"켜기"** 또는 **"Enable"** 선택
   - 비밀번호 입력 요청이 나오면 iPhone 잠금 비밀번호 입력

5. **Xcode에서 다시 빌드:**
   - Developer Mode 활성화 후 Xcode에서 다시 빌드/실행 시도

**참고:**
- iOS 16 이상에서만 이 기능이 있습니다
- Mac의 Developer Mode와는 별개입니다
- 실제 기기에 개발자 앱을 설치하려면 필수입니다

### iOS: "No script URL provided" 오류

**증상:**
```
No script URL provided. Make sure the packager is running or you have embedded a JS bundle in your application bundle.
unsanitizedScriptURLString = (null)

Connection refused
Could not connect to the server.
http://localhost:8081/status
nw_socket_handle_socket_event Socket SO_ERROR [61: Connection refused]
```

**원인:**
- 앱이 `localhost:8081`에 연결하려고 시도
- 실제 기기에서는 `localhost`가 기기 자체를 의미하므로 Mac의 Metro bundler에 연결 불가
- `SKIP_BUNDLING_METRO_IP=1`이 설정되어 있으면 IP 주소가 감지되지 않음
- 또는 Xcode Build Phase에서 IP 감지가 실패함

**원인:**
- Metro bundler가 실행되지 않았거나
- 앱이 Metro bundler에 연결되지 못함 (포트 8081 연결 실패)
- 네트워크 연결 문제

**해결 방법 (순서대로 시도):**

**1. IP 자동 감지 스크립트 사용 (가장 간단한 방법):**
   ```bash
   npm run ios:device
   ```
   또는
   ```bash
   ./scripts/run-ios-device.sh
   ```
   - 자동으로 IP 주소를 감지하고 설정합니다
   - 매번 IP를 입력할 필요가 없습니다

**2. Metro bundler 중지 후 다시 실행:**
   ```bash
   # 기존 Metro bundler 중지 (Ctrl+C)
   # 그 다음:
   npx expo run:ios --device
   ```
   - 이 명령어는 자동으로 IP를 감지하고 Metro bundler를 시작합니다
   - 앱이 올바른 주소에 연결됩니다

**2. Xcode Build Phase에서 `SKIP_BUNDLING_METRO_IP` 제거:**
   
   이전에 Sandbox 오류를 해결하기 위해 `SKIP_BUNDLING_METRO_IP=1`을 설정했다면:
   
   - Xcode → Build Phases → "Bundle React Native code and images"
   - `export SKIP_BUNDLING_METRO_IP=1` 줄을 **제거**하거나 주석 처리
   - 또는 IP를 명시적으로 설정:
   ```bash
   # IP 자동 감지
   export REACT_NATIVE_PACKAGER_HOSTNAME=$(ipconfig getifaddr en0 || ipconfig getifaddr en1)
   ```

**3. 환경 변수로 IP 명시적 설정 (수동 방법):**
   ```bash
   # Mac의 IP 주소 확인
   ipconfig getifaddr en0
   # 또는
   ipconfig getifaddr en1
   
   # IP 주소를 환경 변수로 설정하고 실행
   REACT_NATIVE_PACKAGER_HOSTNAME=192.168.4.71 npx expo run:ios --device
   ```
   ⚠️ **권장하지 않음:** IP가 변경될 때마다 수동으로 입력해야 합니다. 대신 `npm run ios:device` 사용을 권장합니다.

**4. 네트워크 확인:**
   - ✅ 아이폰과 Mac이 **같은 Wi-Fi 네트워크**에 연결되어 있는지 확인
   - ✅ 방화벽 설정 확인
   - ✅ VPN이 켜져 있으면 끄고 시도

**5. USB 연결 사용:**
   - Wi-Fi 연결이 안 되면 USB로 연결:
   ```bash
   npx expo start --dev-client --tunnel
   ```

**4. 앱 완전 종료 후 재실행:**
   - 아이폰에서 앱 완전 종료
   - 다시 실행

**5. Metro bundler 재시작:**
   - 터미널에서 `Ctrl+C`로 중지
   - 캐시 클리어 후 재시작:
   ```bash
   npx expo start --dev-client --clear
   ```

**6. Xcode에서 직접 실행 시 문제:**
   
   ⚠️ **중요:** Xcode에서 직접 실행하면 앱이 `localhost:8081`에 연결하려고 시도합니다.
   실제 기기에서는 Mac의 IP 주소를 사용해야 하므로 연결이 실패합니다!
   
   **해결 방법 1: `npx expo run:ios --device` 사용 (권장)**
   
   Xcode 대신 터미널에서 실행하면 자동으로 올바른 IP를 설정합니다:
   ```bash
   # Metro bundler가 실행 중이어야 함
   npx expo start --dev-client
   
   # 다른 터미널에서:
   npx expo run:ios --device
   ```
   - 이 명령어는 자동으로 Mac의 IP 주소를 감지하고 설정합니다
   - Metro bundler도 자동으로 시작됩니다
   
   **해결 방법 2: Xcode에서 실행하려면**
   
   1. **먼저 Metro bundler 실행:**
      ```bash
      npx expo start --dev-client
      ```
      - 터미널에 "Metro waiting on..." 메시지 확인
      - IP 주소 확인 (예: `192.168.4.71:8081`)
   
   2. **Xcode Build Phase 수정:**
      - Xcode → Build Phases → "Bundle React Native code and images"
      - 스크립트 맨 위에 추가:
      ```bash
      export SKIP_BUNDLING_METRO_IP=1
      export REACT_NATIVE_PACKAGER_HOSTNAME=192.168.4.71
      ```
      - ⚠️ IP 주소는 매번 변경될 수 있으므로 권장하지 않음
   
   3. **Xcode에서 실행:**
      - Product → Run (⌘R)
   
   **가장 간단한 방법:**
   ```bash
   # 한 번에 실행 (Metro bundler 자동 시작 + 올바른 IP 설정)
   npx expo run:ios --device
   ```

### iOS: "Device is busy" 또는 "Timed out" 오류

**증상:**
```
error: Device is busy (Waiting to reconnect to Siro)
Timed out waiting for all destinations matching the provided destination specifier
```

**해결 방법 (순서대로 시도):**

1. **기기 잠금 해제**
   - iPhone 화면이 잠겨있으면 잠금 해제

2. **USB 연결 확인**
   - 케이블을 뽑았다가 다시 연결
   - iPhone에서 "이 컴퓨터를 신뢰" 다시 확인

3. **Xcode에서 기기 상태 확인**
   ```bash
   open ios/TodayCheck.xcworkspace
   ```
   - Xcode 상단에서 기기 선택 드롭다운 확인
   - 기기 옆에 경고 아이콘(⚠️)이 있으면:
     - 기기 이름 클릭 → "Use for Development" 선택
     - 또는 Window → Devices and Simulators → 기기 선택 → "Use for Development"

4. **기기 재연결**
   - Xcode → Window → Devices and Simulators
   - 기기 선택 → "Unpair Device" (필요시)
   - USB 재연결 → 다시 페어링

5. **Xcode에서 직접 빌드**
   ```bash
   open ios/TodayCheck.xcworkspace
   ```
   - Xcode 상단에서 기기 선택
   - Product → Clean Build Folder (⇧⌘K)
   - Product → Build (⌘B)
   - Product → Run (⌘R)

6. **다른 USB 포트/케이블 시도**
   - USB 3.0 포트 사용 권장
   - 케이블이 데이터 전송을 지원하는지 확인 (충전 전용 케이블 X)

### Android: "Device unauthorized" 오류

**증상:**
```
adb: device unauthorized
```

**해결:**
1. 기기에서 "USB 디버깅 허용" 팝업 확인
2. "항상 이 컴퓨터에서 허용" 체크 → 확인
3. `adb devices`로 연결 확인:
   ```bash
   adb devices
   ```
   - `unauthorized`가 보이면 기기에서 다시 허용

### iOS: Sandbox 권한 오류 (ip.txt)

**증상:**
```
Sandbox: bash deny(1) file-write-create .../ip.txt
error: Operation not permitted
```

**원인:**
- React Native 스크립트가 앱 번들 안에 `ip.txt` 파일을 쓰려고 시도
- iOS Sandbox가 앱 번들 내부 파일 쓰기를 차단

**해결 방법 (권장):**

**Xcode Build Phase에서 스크립트 수정:**

1. Xcode에서 프로젝트 열기:
   ```bash
   open ios/TodayCheck.xcworkspace
   ```

2. Build Phase 수정:
   - 왼쪽 프로젝트 네비게이터에서 **TodayCheck** 프로젝트 선택
   - **TARGETS** → **TodayCheck** 선택
   - 상단 탭에서 **Build Phases** 클릭
   - **"[CP] Copy Pods Resources"** 스크립트 찾기
   - 스크립트 내용 맨 **위**에 다음 줄 추가:
     ```bash
     # Fix Sandbox permission issue
     export RESOURCES_TO_COPY="${TARGET_BUILD_DIR}/resources-to-copy-${TARGETNAME}.txt"
     ```
   - 기존 스크립트는 그대로 유지:
     ```bash
     "${PODS_ROOT}/Target Support Files/Pods-TodayCheck/Pods-TodayCheck-resources.sh"
     ```

3. **"Bundle React Native code and images"** 스크립트도 수정:
   - 스크립트 내용 맨 위에 추가:
     ```bash
     export SKIP_BUNDLING_METRO_IP=1
     ```

4. Clean & Build:
   - Product → Clean Build Folder (⇧⌘K)
   - Product → Build (⌘B)
   - Product → Run (⌘R)

**근본 해결: Xcode Build Settings에서 Sandbox 비활성화**

여러 Sandbox 오류가 계속 발생하면:

**방법 1: Build Settings에서 검색**

1. Xcode에서 프로젝트 열기:
   ```bash
   open ios/TodayCheck.xcworkspace
   ```

2. Build Settings 수정:
   - 왼쪽 프로젝트 네비게이터에서 **TodayCheck** 프로젝트 선택
   - **TARGETS** → **TodayCheck** 선택
   - 상단 탭에서 **Build Settings** 클릭
   - 검색창에 다음 중 하나를 시도:
     - **"Sandbox"** (가장 일반적)
     - **"ENABLE_USER_SCRIPT_SANDBOXING"** (빌드 설정 키)
     - **"User Script"**
   - 찾은 설정의 값을 **No** 또는 **Disabled**로 변경

**방법 2: Build Phases에서 스크립트에 환경 변수 추가 (더 확실한 방법)**

1. Xcode에서 프로젝트 열기:
   ```bash
   open ios/TodayCheck.xcworkspace
   ```

2. Build Phases 수정:
   - 왼쪽 프로젝트 네비게이터에서 **TodayCheck** 프로젝트 선택
   - **TARGETS** → **TodayCheck** 선택
   - 상단 탭에서 **Build Phases** 클릭
   - **모든 스크립트** (특히 "[CP] Copy Pods Resources"와 "Bundle React Native code and images")의 맨 위에 추가:
     ```bash
     # Disable Sandbox restrictions
     export ENABLE_USER_SCRIPT_SANDBOXING=NO
     ```

3. Clean & Build:
   - Product → Clean Build Folder (⇧⌘K)
   - Product → Build (⌘B)
   - Product → Run (⌘R)

**대안 방법:**

1. **DerivedData 정리**
   ```bash
   rm -rf ~/Library/Developer/Xcode/DerivedData/TodayCheck-*
   ```

2. **스크립트 직접 수정 (임시)**
   - `node_modules/react-native/scripts/react-native-xcode.sh` 27번째 줄:
     ```bash
     # 기존: echo "$IP" > "$DEST/ip.txt"
     # 수정: echo "$IP" > "$HOME/Documents/ip.txt" 2>/dev/null || true
     ```
   - ⚠️ 주의: `npm install` 시 덮어씌워질 수 있음

---

## 요약

| 환경 | 실행 방법 | Burnt 토스트 |
|------|-----------|--------------|
| **웹** | `npx expo start --web` | ✅ 동작 (Toaster 사용) |
| **Expo Go** | `npx expo start` 후 Expo Go 앱으로 스캔 | ❌ Burnt 미지원 |
| **iOS/Android (네이티브)** | `prebuild` → `run:ios` / `run:android` | ✅ 동작 |

즉, **폰/에뮬레이터에서 토스트를 보려면** 반드시 `prebuild` 후 `run:ios` 또는 `run:android`로 띄워야 합니다.
